{% extends 'base.html.twig' %}

{% block title %}éditer {{property.title}}{% endblock %}

{% block body %}
<div class="container">
    <h1> Editer {{property.title}}</h1>

    {{ form_start(form) }}
        <div class="row">
            <div class="col-md-8">{{ form_row(form.title) }}</div>
            <div class="col-md-4">{{ form_row(form.sold) }}</div>
            <div class="col-md-12">{{ form_row(form.description) }}</div>
        </div>
        <div class="row">
            <div class="col-md-4">{{ form_row(form.surface) }}</div>
            <div class="col-md-4">{{ form_row(form.price) }}</div>
            <div class="col-md-4">{{ form_row(form.rooms) }}</div>
            <div class="col-md-4">{{ form_row(form.bedrooms) }}</div>
            <div class="col-md-4">{{ form_row(form.heat) }}</div>
            <div class="col-md-4">{{ form_row(form.floor) }}</div>
        </div>
        <div class="row">
            <div class="col-md-12">{{ form_row(form.address) }}</div>
            <div class="col-md-6">{{ form_row(form.city) }}</div>
            <div class="col-md-6">{{ form_row(form.postal_code) }}</div>
        </div>
        
    
    <button class="btn btn-primary"> Modifier </button>
    {{ form_end(form) }}
</div>
{% endblock %}
